services:

  app:

    build: .

    container_name: app

    expose:
      - "3000"

    volumes:
      - .:/app
      - /app/node_modules

    command: node server.js

    depends_on:
      - redis

  redis:

    image: redis:latest

    container_name: redis
