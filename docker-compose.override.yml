services:

  nginx:

    image: nginx:latest

    container_name: nginx

    ports:
      - "80:80"
      - "443:443"

    volumes:
      - ./nginx/nginx.local.conf:/etc/nginx/nginx.conf:ro
      - ./cert:/etc/cert:ro

    depends_on:
      - app
